#include <PieceMove.h>

PieceMove::PieceMove(){
  Position position = {-1,-1};
  this->source = position;
  this->destination = position;
}

Position PieceMove::addMove(Position position){
  return source;
}